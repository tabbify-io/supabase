[app]
name = "studio"

[build]
kind = "docker"
context = "."
dockerfile = "apps/studio/Dockerfile"

[runtime]
memory_mb = 1024
vcpus = 1

[[runtime.ports]]
port = 3000
protocol = "http"
expose = "public"
primary = true
